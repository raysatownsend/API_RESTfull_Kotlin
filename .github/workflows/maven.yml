name: Continuous Integration with Github Actions

on:
  push:
    branches: 
      - secnd_branch
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Setup Java JDK
      uses: actions/setup-java@v2
      with:
        java-version: '20'
        distribution: 'adopt'
        cache: maven
    
    - name: Build Application Jar
      working-directory: ./API_RESTfull_Kotlin/tree/secnd_branch
    
    - name: Docker Compose Build
      working-directory: ./API_RESTfull_Kotlin/tree/secnd_branch
      run: docker compose build
    
    - name: Push to Docker Hub Container registry
      run: |
        docker tag ${{ secrets.DOCKER_USERNAME }}/client_database_test:latest ${{ secrets.DOCKER_USERNAME }}/client_database_test:${{github.run_id}}
        
        docker push ${{ secrets.DOCKER_USERNAME }}/client_database_test:${{github.run_id}}
        docker push ${{ secrets.DOCKER_USERNAME }}/client_database_test:latest
